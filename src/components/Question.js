import React from 'react';

const Question = () => {
    return (
        <div>
            <p>React work one directional way.React is like tree.Its work top to bottom.
            </p>
            <p>Props are used to pass data from one component to another. The state is a local data storage that is local to the component only and cannot be passed to other components.</p>
            <p> React has another special built-in object called state, which allows components to create and manage their own data.</p>
        </div>
    );
};

export default Question;